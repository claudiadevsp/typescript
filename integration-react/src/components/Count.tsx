import React, { Component } from 'react'
import CountValue from './CountValue'

interface CountProps {
    inicialValue?: number
}

export default 
    class Count<CountProps> extends Component {
        render() {
            return {
                
            }
        }
}